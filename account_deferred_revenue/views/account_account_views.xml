<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record model="ir.actions.server" id="action_account_aml_to_deferred_revenue">
        <field name="name">Turn as a deferred revenue</field>
        <field name="model_id" ref="account.model_account_move_line"/>
        <field name="binding_model_id" ref="account.model_account_move_line" />
        <field name="state">code</field>
        <field name="code">
if records:
    action = records.turn_as_deferred_revenue()
        </field>
    </record>
    <record model="ir.actions.server" id="action_account_aml_to_deferred_expense">
        <field name="name">Turn as a deferred expense</field>
        <field name="model_id" ref="account.model_account_move_line"/>
        <field name="binding_model_id" ref="account.model_account_move_line" />
        <field name="state">code</field>
        <field name="code">
if records:
    action = records.turn_as_deferred_expense()
        </field>
    </record>
</odoo>
