<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">

    <record id="mail_template_data_intervention_details" model="mail.template">
        <field name="name">Task: Intervention Scheduled</field>
        <field name="model_id" ref="project.model_project_task"/>
        <field name="subject">Your intervention is scheduled ${object.planned_date_begin and object.planned_date_end and 'from the ' + format_datetime(object.planned_date_begin, tz=object.partner_id.tz, lang_code=object.partner_id.lang) + ' to the ' + format_datetime(object.planned_date_end, tz=object.partner_id.tz, lang_code=object.partner_id.lang) or '' | safe}</field>
        <field name="use_default_to" eval="True"/>
        <field name="body_html" type="html">
<div>
    % set date_begin = format_datetime(object.planned_date_begin, tz=object.partner_id.tz, lang_code=object.partner_id.lang)

    % set date_end = format_datetime(object.planned_date_end, tz=object.partner_id.tz, lang_code=object.partner_id.lang)

    Dear ${object.partner_id.name or 'customer'},<br/><br/>
    % if date_begin and date_end:
        Your ${object.name} intervention is scheduled from the ${date_begin} to the ${date_end}.
    % else:
        Your ${object.name} intervention is scheduled.
    % endif
    <br/><br/>
    Best regards,
    % if user.signature
        <br/>
        ${user.signature | safe}
    % endif
</div>
        </field>
        <field name="lang">${object.partner_id.lang}</field>
        <field name="auto_delete" eval="True"/>
    </record>

</odoo>
