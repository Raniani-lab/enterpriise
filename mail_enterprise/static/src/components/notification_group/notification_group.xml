<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="mail_enterprise.NotificationGroup" t-inherit="mail.NotificationGroup" t-inherit-mode="extension" owl="1">
        <!--
            This template wraps the NotificationGroup in an ActionSwiper to open the dialog to cancel action
        -->
        <xpath expr="//*[hasclass('o_NotificationGroup')]" position="after">
            <ActionSwiper
                onRightSwipe="messaging and messaging.device.isMobile and group and group.notifications.length > 0 ?
                    {
                        action: () => group and group.notifications.length > 0 and group.openCancelAction(),
                        icon: 'fa-times-circle',
                        bgColor: 'bg-warning',
                    } : undefined"
            />
        </xpath>
        <xpath expr="//ActionSwiper" position="inside">
            <xpath expr="//*[hasclass('o_NotificationGroup')]" position="move"/>
        </xpath>
    </t>
</templates>
