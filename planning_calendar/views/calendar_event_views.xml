<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="calendar_event_generate_shifts" model="ir.actions.server">
        <field name="name">Generate Shifts</field>
        <field name="model_id" ref="calendar.model_calendar_event"/>
        <field name="binding_model_id" ref="calendar.model_calendar_event"/>
        <field name="binding_view_types">list,form</field>
        <field name="groups_id" eval="[(4, ref('planning.group_planning_manager'))]" />
        <field name="state">code</field>
        <field name="code">action = records.action_create_slots()</field>
    </record>
</odoo>
