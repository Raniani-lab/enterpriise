<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="sale_order_log_view_tree" model="ir.ui.view">
        <field name="name">sale.order.log.tree</field>
        <field name="model">sale.order.log</field>
        <field name="arch" type="xml">
            <tree decoration-muted="event_type == '2_snapshot'"
                  decoration-success="event_type == '1_change' and amount_signed &gt; 0"
                  decoration-danger="event_type == '3_churn'"
                  decoration-warning="event_type == '1_change' and amount_signed &lt; 0"
                  create="0" delete="0">
                <field name="order_id"/>
                <field name="event_date"/>
                <field name="event_type"/>
                <field name="user_id"/>
                <field name="team_id"/>
                <field name="amount_signed"/>
                <field name="recurring_monthly"/>
                <field name="subscription_state" string="Status" widget="badge"
                   decoration-success="subscription_state == '3_progress'"
                   decoration-warning="subscription_state == '4_paused'"
                   decoration-danger="subscription_state == '6_churn'"
                   decoration-primary="subscription_state == '7_upsell'"
                   decoration-info="subscription_state in ['1_draft', '2_renewal']"
                   readonly="1" optional="hide"/>
                <field name="currency_id" invisible="1"/>
            </tree>
        </field>
    </record>

    <record id="sale_order_log_view_graph" model="ir.ui.view">
        <field name="name">sale.order.log.graph</field>
        <field name="model">sale.order.log</field>
        <field name="arch" type="xml">
            <graph string="MRR Analysis" disable_linking="1" sample="1" stacked="0">
                <field name="order_id"/>
                <field name="event_type"/>
                <field name="event_date" interval="day"/>
                <field name="amount_signed" type="measure"/>
                <field name="recurring_monthly" type="measure"/>
            </graph>
        </field>
    </record>

    <record id="action_sale_order_log" model="ir.actions.act_window">
        <field name="name">MRR Analysis</field>
        <field name="view_mode">tree,graph</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">sale.order.log</field>
    </record>
</odoo>
