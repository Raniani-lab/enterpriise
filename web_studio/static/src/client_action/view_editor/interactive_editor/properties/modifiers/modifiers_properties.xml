<templates>
    <t t-name="web_studio.ViewEditor.InteractiveEditorProperties.Modifiers">
        <Property
            t-if="this.isAttributeSupported('invisible')"
            isConditional="true"
            name="'invisible'"
            type="'boolean'"
            value="props.node.attrs.invisible || props.node.attrs.column_invisible"
            onChange.bind="onChangeModifier"
        >
            Invisible
        </Property>

        <Property
            t-if="this.isAttributeSupported('required')"
            isConditional="true"
            name="'required'"
            type="'boolean'"
            value="props.node.attrs.required"
            onChange.bind="onChangeModifier"
        >
            Required
        </Property>

        <Property
            t-if="this.isAttributeSupported('readonly')"
            isConditional="true"
            name="'readonly'"
            type="'boolean'"
            value="props.node.attrs.readonly"
            onChange.bind="onChangeModifier"
        >
            Readonly
        </Property>
    </t>
</templates>
